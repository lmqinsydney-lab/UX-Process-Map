# 素材投放目录

把「图二」原始大图（完整交付流程图）放到本目录即可，文件名随意（建议 `flow-full.png`）。

阶段 3 我会：
1. 用 `scripts/crop-pages.mjs` 按裁切框批量裁出各「页面 × 状态」；
2. 覆盖 `public/assets/pages/<pageId>/<stateId>` 下的占位图；
3. 对照真实截图校准热区坐标。

后续 Figma 高清图导出命名规则见 `docs/figma-export-guide.md`（阶段 3 产出）。
